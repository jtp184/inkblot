#!/usr/bin/env ruby

abort "No .raspi-hostname file" unless File.exist?('.raspi-hostname')

rpi = File.read('.raspi-hostname')

if ARGV.empty?
	exec("ssh pi@#{rpi}")
else
	exec("ssh pi@#{rpi} #{ARGV.join(' ')}")
end
